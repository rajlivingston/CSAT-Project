# CSAT Project Backend

Customer Satisfaction (CSAT) reporting application.

## Setup

1. Install dependencies:
   ```bash
   poetry install
   ```

2. Configure `.env` based on `.env.example`.

3. Run the application:
   ```bash
   ./start_gunicorn.sh
   ```
